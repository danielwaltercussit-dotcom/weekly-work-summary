---
name: weekly-work-summary
description: 把一周的工作整理成给上级看的周报(Word/.docx)。素材可来自 git 提交记录、项目文件改动时间线、以及用户口述/粘贴的内容。当用户说"总结一下我这周的工作""写个周报""这周干了啥整理一下""帮我汇报本周进展""weekly report""周总结"等,或在周五/周末提到要向领导汇报工作时,都应使用本 skill。即使用户没有明说"周报"二字,只要是把一段时间(一周左右)的工作产出整理成可上交的汇报材料,就用它。注意:这是面向上级的正式汇报,重点是成果与影响,不是操作流水账。
---

# Weekly Work Summary(一周工作总结 → 周报)

把零散的一周工作整理成一份**给上级看的、专业、可直接上交的周报**,默认输出 Word(.docx)。周报必须先给出一段非技术读者也能读懂的摘要,再展开成果和计划。

## 这份 skill 的灵魂:写"成果",不写"流水账"

周报最常见、也最致命的失败模式,是退化成一份操作日志——"改了 X 文件、跑了 Y 脚本、提交了 Z 次"。上级不关心你敲了哪些键,他关心的是:**事情推进到哪了?产出了什么?有没有风险需要他介入?** 你的核心工作就是把底层操作(commit、文件改动、调试过程)**向上抽象成成果和影响**。

一个简单的检验:周报里每一条,问自己"这条让领导知道了什么他之前不知道、且对决策有用的信息?"如果答案是"知道我很忙",那这条就要重写。

对照感受一下:

- 流水账(不要):"修改了 build_pc_pdf.py,重新生成了主机配置 PDF,又改了 merge_pdfs.py。"
- 成果导向(要):"完成主机配置方案的文档化,输出可交付的配置方案 PDF,已可用于采购评审。"

- 流水账(不要):"git 提交了 3 次,新增 298 个文件。"
- 成果导向(要):"将 Simulink agent 项目纳入 Git 版本管理,建立基线,为后续多人协作和回溯打下基础。"

摘要要写给**不熟悉代码推进细节的读者**。不要假设对方理解 commit、stage、helper、routing 这些词;先用 2~4 句话说明本周主线、产出价值、当前状态和需要关注的点,再在下面的成果条目里放具体模块名。

## 绝不编造(最重要的硬规则)

这份周报会送到领导面前,**任何编造的进度都可能让用户当众出丑或做错决策**。所以:

- 只写你能从素材(git、文件、用户口述)中**确证**的事。
- 素材不足、或某件事的状态/影响你拿不准时,**宁可问用户,也不要猜**。比如:你能看到某文件被改动,但不知道这件事完成没、属于哪个项目目标——这时该问,而不是脑补一个"已完成"。
- 不要凭空拔高。把 commit 抽象成成果是合理的提炼;把"改了个参数"写成"重大技术突破"是欺骗。

宁可周报短一点、诚实一点,也不要长而虚。

## 工作流程

### 第 1 步:确定范围与素材来源

先搞清楚要总结**哪个时间段、哪个/哪些项目**。默认是"最近 7 天"。如果用户没指明项目,看上下文或直接问。

素材有三个来源,**优先级从高到低、但要交叉印证**:

1. **用户口述 / 粘贴的内容** —— 最高权重。用户说"这周主要在搞 X",那 X 就是主线,git 和文件只是佐证。
2. **Git 提交记录** —— 客观但可能稀薄。先确认用户的项目在哪、用没用 git;若不确定,问用户或在常见位置扫一下。注意很多项目**不是 git 仓**,git log 也可能只有寥寥几条甚至全是 "Initial commit",这时绝不能因为 git 空就说"这周没干活",要转向文件改动和用户口述。
3. **项目文件改动时间线** —— 当 git 稀薄时这是主力。扫描最近 7 天被修改/新建的文件,还原出工作脉络。

采集命令见 `references/data-collection.md`,需要时读它。

### 第 2 步:采集 + 提炼

按上面的来源采集原始素材后,做**提炼**而非罗列:

- 把同一件事的多个 commit / 多个文件改动**合并成一条成果**。(例如 10 个关于 docx 生成的 commit → "完成周报自动生成功能")
- 给每条成果判断**状态**:已完成 / 进行中 / 受阻。受阻的要写清卡在哪、是否需要上级支持——这是周报对领导最有价值的部分。
- 如果素材里能看出**量化结果**(文件数、测试通过、性能提升数字),带上,因为数字最有说服力。但不许编数字。
- 先写 `summary`:用业务/项目语言解释"这周整体推进了什么、为什么重要、现在处于什么状态"。摘要不能新增成果,只能概括下方有证据支撑的内容。

### 第 3 步:按结构组织内容

见下方"周报结构"。如果信息有缺口(比如不知道某事的下周计划),在生成前**一次性把要问的都问清楚**,别挤牙膏式反复打断用户。摘要缺口不需要单独问用户;从已确认的成果、进行中事项、阻塞和下周计划里保守概括即可。

### 第 4 步:生成 .docx

用户要的是 Word 文档。用本 skill 自带的脚本生成,省得每次重写。脚本就在本 skill 目录下的 `scripts/build_docx.py`——用它相对本 SKILL.md 的路径调用,不要写死任何机器上的绝对路径:

```bash
python <skill_dir>/scripts/build_docx.py \
  --json <content.json 路径> \
  --out  <周报_YYYY-MM-DD.docx 路径>
```

`<skill_dir>` 就是本 SKILL.md 所在的目录。脚本接收一个描述周报内容的 JSON,产出排版规整、带中文字体与配色的 .docx。JSON 的字段结构和示例见 `references/docx-content-schema.md`。生成前先把内容写成那个 JSON,再调脚本。

依赖 `python-docx`(`pip install python-docx`)。若环境里没有,先装它;装不上时的降级方案:用任意可用的方式生成 Word(比如直接用 python-docx 手写,或先给用户 Markdown 预览再转 Word)。排版样式集中在 `scripts/docx_style.py`,要换主题色/字体改那里即可。

### 第 5 步:交付

把生成的 .docx 路径告诉用户。如果这周明显有"受阻/需要上级决策"的事项,在对话里**额外口头提醒一句**,因为这是领导最该第一时间看到的。

## 周报结构

默认用下面这个面向上级的结构。可按用户单位习惯调整,但"本周成果"和"下周计划"是骨架,不要省。

```
# [姓名] 周报(YYYY-MM-DD ~ YYYY-MM-DD)

## 摘要
(2~4 句,面向不懂代码推进细节的读者,说明本周主线、项目价值、整体状态和需关注事项。)

## 本周核心成果
(3~6 条,每条一句话讲清"做成了什么 + 影响/价值"。最重要的放最前。)

## 进行中事项
(还没收尾的,写清当前进度和预计完成时间。)

## 问题与需要的支持
(受阻事项、需要上级决策或协调的资源。没有就写"本周无"。)

## 下周计划
(3~5 条,聚焦可交付的目标,而非"继续做 X"。)
```

**为什么是这个顺序**:领导时间有限,结论先行。摘要先把技术推进翻译成项目状态;成果随后让他三秒抓住重点;"问题与支持"单独成节,是因为这是周报真正驱动行动的部分,埋在流水账里就浪费了。

## 语气与篇幅

- 简洁、客观、结论先行。一条成果一两句话,不堆形容词。
- 摘要避免代码名词堆叠。必须出现技术名词时,后面接一句它带来的项目价值。
- 用第一人称或省略主语的陈述句("完成了…""推进了…"),别用"我觉得""可能大概"这类虚词。
- 整篇控制在领导一分钟能读完的量。宁缺毋滥。
- 中文周报用中文;若用户的工作语境是英文,用英文。

## 边界情况

- **素材几乎为空**(git 没提交、近 7 天没文件改动、用户也没说):不要硬编一份周报。如实告诉用户"我没找到这周的工作痕迹",请他口述一下做了什么,再据此整理。
- **跨多个项目**:按项目分组组织"本周核心成果",每个项目一个小标题,避免混在一起读不清。
- **用户只要 Markdown 不要 Word**:那就只给 Markdown,别多此一举生成 .docx。尊重用户当下要的形式。
- **用户给的时间段不是一周**(如"这个月""这两周"):照他说的范围来,标题和采集窗口相应调整。
